// BaaS 配置 — 由代码生成引擎自动注入
const config = {
  baseURL: '{{baasBaseURL}}',
  apiKey: '{{apiKey}}',
  projectId: '{{projectId}}',
  appName: '{{appName}}',
  primaryColor: '{{primaryColor}}',
};

export default config;
